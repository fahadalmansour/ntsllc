# Dependencies
node_modules
node_modules/
rst/node_modules/

# Build output
rst/dist/
dist/

# Environment
.env
.env.local
.env.production

# Editor
.DS_Store
.vscode/
*.swp

# Logs
*.log
npm-debug.log*

# Type cache
*.tsbuildinfo

# Local design / preview artifacts
design/
render_tmp_puppeteer_*/

# Supabase secrets — never commit (audit BLOCKER #1).
# The `info.tsx` files now read from env (`.env.local`); .env files
# below are gitignored. The legacy top-level `utils/supabase/` path
# (deleted in this commit) is also locked out as defense-in-depth.
utils/supabase/
.env
.env.local
.env.production
.env.development
